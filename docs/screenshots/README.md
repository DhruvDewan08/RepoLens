# Screenshots

Committed in this folder:

- `architecture.svg` — pipeline diagram (used in the root README)

Add **real** Streamlit captures (not mockups):

- `chat.png` — Chat tab
- `impact.png` — Impact graph + source inspector
- `folders.png` — File graph or Functions tree

`Win + Shift + S` while `streamlit run streamlit_app.py` is open.
